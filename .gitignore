# PlatformIO build artifacts
.pio
.venv/

# VS Code — keep extensions.json for collaborator onboarding
.vscode/*
!.vscode/extensions.json

# Local credentials — copy include/secrets.example.h → include/secrets.h
include/secrets.h

# Font source files — headers in include/fonts/ are committed,
# TTFs can be re-downloaded if regeneration is needed
fonts_tff/

# OS files
.DS_Store
Thumbs.db

# Nested working copy of the firmware (has its own .git) — not part of this repo.
# If you intend to track it, remove this line and add it as a proper submodule instead.
calendar-dashboard/
